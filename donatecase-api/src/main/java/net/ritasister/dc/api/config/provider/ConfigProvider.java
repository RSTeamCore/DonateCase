package net.ritasister.dc.api.config.provider;

/**
 * A provider interface for configuration resources.
 *
 * @param <P> the type of the provider
 * @param <C> the type of the configuration resource
 * @since 1.0.21
 */
public interface ConfigProvider<P, C> extends ResourceProvider<P, C> {

}
